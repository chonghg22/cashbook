<?xml version="1.0" encoding="UTF-8" ?> 

<!DOCTYPE mapper 
	PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
	"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd"> 
<mapper namespace="com.gdu.cashbook.mapper.BoardMapper">

	<select id="selectBoardList" parameterType="com.gdu.cashbook.vo.Board" resultType="com.gdu.cashbook.vo.Board">
		SELECT
		board_no as boardNo,
		member_id as memberId,
		board_title boardTitle,
		board_content boardContent,
		board_comment boardComment,
		board_date boardDate
		FROM cashbook_board
	</select>
	
	<insert id = "insertBoard" parameterType = "com.gdu.cashbook.vo.Board">
	INSERT INTO
	cashbook_board(
	member_id,
	board_title,
	board_content,
	board_date
	)
	VALUES(
	#{memberId},
	#{boardTitle},
	#{boardContent},
	NOW()
	)
	</insert>

</mapper>
